export class User {
    constructor(
        private name:string,
        private email:string,
        private phone:string,
        private topic:string,
        private timePreference: string,
        private subscribe: boolean

    ){}
}
